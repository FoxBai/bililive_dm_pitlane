# Notice

This repository is a Pitlane-oriented branch based on:

- `copyliu/bililive_dm`: https://github.com/copyliu/bililive_dm

The original project is licensed under WTFPL. The original license text is preserved in `LICENSE.txt`.

Pitlane-specific assets were taken from the `FoxBai/pitlane_danmaku` asset set and are stored under `assets/`.

The Windows native implementation under `windows-native/PitlaneDanmaku.Windows` is newly written for this branch. It uses the public Bilibili danmaku protocol behavior as a compatibility reference and does not directly copy the previous `FoxBai/pitlane_danmaku` Windows implementation.
